spring:
  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      database: learn_query
      command:
        host: 127.0.0.1
        port: 27017
        database: learn_command
        username:
        password:
  profiles:
    active: local
server:
  port: 8443
  ssl:
    protocol: TLS
    key-store: classpath:https.keystore
    key-store-password: yuweiye
    key-store-type: JKS
    key-alias: https